import { GiDraggableDirective } from './gi-draggable.directive';

describe('GiDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new GiDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
